// Barrel exports for core modules (ESM facade over existing globals)
// Barrel exports mapping to legacy globals; ESM modules are available under *.esm.js
export const AppConfig = window.AppConfig;
export const StorageCore = window.StorageCore;
export const Utils = window.Utils;


